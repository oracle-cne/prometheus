---
TagFormat: v(?P<major>[0-9])\.(?P<minor>[0-9]+)\.(?P<patch>[[:digit:]]+)
BranchTemplate: feature/${major}.${minor}.${patch}
Comparator: '0'
MasterBranchTemplate: oracle/release/${major}.${minor}.${patch}
TemplateDirectory: "v${major}.${minor}"
SemverTagFilter: ">= 3.5.0"
